import { AdminLogin } from "@/components/admin-login"

export const metadata = {
    title: "Admin Login - Contact Dashboard",
    description: "Login to access admin dashboard",
}

export default function AdminLoginPage() {
    return <AdminLogin />
}
